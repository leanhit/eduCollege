# Backend Service for eduCollege University System
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: educollege
  labels:
    app: educollege
    component: backend
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/actuator/prometheus"
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  - name: https
    port: 443
    targetPort: 8080
    protocol: TCP
  selector:
    app: educollege
    component: backend
---
# Headless service for gRPC
apiVersion: v1
kind: Service
metadata:
  name: backend-grpc
  namespace: educollege
  labels:
    app: educollege
    component: backend
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
  selector:
    app: educollege
    component: backend
